{
  "name": "koi-design",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "audit:browser": "vp run @koi/web#build && node scripts/capture-browser-audit.mjs",
    "build": "vp run -r --concurrency-limit 2 build",
    "check": "vp run -r --concurrency-limit 2 build && vp run -r --concurrency-limit 2 check",
    "dev": "vp run -r --concurrency-limit 2 build && vp run @koi/web#dev",
    "licenses:generate": "node scripts/generate-license-evidence.mjs",
    "ready": "vp run -r --concurrency-limit 2 build && vp run -r --concurrency-limit 2 check && vp run -r --concurrency-limit 2 test && vp exec playwright test --workers=1",
    "test": "vp run -r --concurrency-limit 2 build && vp run -r --concurrency-limit 2 test",
    "test:e2e": "vp run -r --concurrency-limit 2 build && vp exec playwright test --workers=1",
    "webmcp:manifest": "vp run @koi/web#build && node scripts/capture-webmcp-tools.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "catalog:",
    "@playwright/test": "1.62.1",
    "@types/node": "catalog:",
    "vite": "npm:@voidzero-dev/vite-plus-core@0.3.0",
    "vite-plus": "0.3.0"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.21.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "pnpm@11.21.0"
}

# Generate with: openssl rand -hex 32
KOI_AUTH_TOKEN=

# The Compose port is bound to localhost by default.
KOI_PORT=8787

# Set this to the external HTTPS origin when deploying behind a reverse proxy.
KOI_PUBLIC_ORIGIN=
